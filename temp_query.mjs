const { open } = require('node:fs/promises'); const db = require('better-sqlite3'); try { const d = new db('c:/Users/User/Downloads/TenXConvo_v7_with_EasyPaisa/TenXConvo_output/tenxconvo_v2/src/tenxconvo.api/tenxconvo_dev.db', {readonly:true}); console.log(JSON.stringify(d.prepare('SELECT Id, UserName, Email, LoginId, IsActive, RoleId FROM Users').all(), null, 2)); d.close(); } catch(e) { console.error(e.message); }
